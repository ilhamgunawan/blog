name: ci

on:
  pull_request:
    branches:
      - main

jobs:
  Unit-Test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Dependencies
        run: npm ci
      - name: Start Unit Test
        run: npm run test
